import ar from "./ar";
import en from "./en";
import ur from "./ur";

// Adding a language later is: write resources/<code>.js, add it here, and
// register it in ../locales.js. Nothing else in the app needs to change.
export const resources = { ar, en, ur };
